package LeanJDK111;

public class Features1 {

    /**
     *      1、嵌套类可见性支持
     *      嵌套类可见性支持，这里主要增加了对于嵌套类private的支持。
     *      private 属性， private方法
     *
     *
     *      2、用于 Lambda 参数的局部变量语法
     *      用于 Lambda 参数的局部变量语法简单来说就是支持的类型推导
     *      var x = new A();
     *      for (var x : xs) { ... }
     *      try (var x = ...) { ... } catch ...
     *
     *
     */
}
