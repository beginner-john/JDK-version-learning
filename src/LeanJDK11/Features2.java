package LeanJDK111;

public class Features2 {

    /**
     *      1、HTTP Client
     *      以前我们不得不引入三方apache commons　的HTTPClient或者OKHTTP等三方库。
     *      从目前来看，jdk10对于这块的功能支持还是不错的，目前版本已经支持HTTP1.1、HTTP2、websocket等常用的基于http的协议，
     *      并支持了了同步、异步、响应式等交互方式。当前版本的实现还是比较简单，没有对于常用restful、content-type的封装支持。
     *
     *
     *       2、动态文件常量/低开销的 Heap Profiling
     *       动态文件常量主要增加了 CONSTANT_Dynamic　这种常量池形式，主要用于丰富常量池API相关接口，并优化常量生成策略。
     *
     *
     *       3、TLS 1.3支持
     *       TLS 主要是对于https的新标准的一些支持。
     *
     */
}
